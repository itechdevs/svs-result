import TeacherSetupClient from "@/components/admin/TeacherSetupClient";

export const metadata = { title: "Setup Teachers - Admin" };

export default function TeacherSetupPage() {
  return <TeacherSetupClient />;
}
